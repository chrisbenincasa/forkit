%div{:class => "recipe_list #{type}_list"}
  - recipes.each do |recipe|
    %div.recipe_picture
      - if recipe.image?
        = link_to image_tag(recipe.image_url(:thumb)), recipe
      - else
        = link_to image_tag('default.png', :size => '150x150'), recipe
%div.clearfix